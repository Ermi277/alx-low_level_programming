#include <stdio.h>
/**
 * main - printf
 *
 * Resource: with proper grammar, but the outcome is a piece of art,
 **/
int main(void)
{
printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
