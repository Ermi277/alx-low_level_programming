#include <stdio.h>

/**
 * main - task0header
 *
 *
 * Return: 0
 **/

int _putchar(char a, char b, char c, char d, char e, char f, char g, char h)
{
	putchar(a);
	putchar(b);
	putchar(c);
	putchar(d);
	putchar(e);
	putchar(f);
	putchar(g);
	putchar(h);
	putchar('\n');
}
